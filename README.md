# TRAFFIX — Smart Routing Copilot

New application layer (app/, frontend/) wrapping the existing, untouched
SUMO/TraCI/ML system that already lives at the project root (trafficx_*.py,
train_xgboost_v*.py, datasets, models/, scenarios/).

See docs/setup.md to run the project, docs/architecture.md for how the
new layer integrates with the existing engine, and docs/demo_script.md
for the hackathon demo sequence.
