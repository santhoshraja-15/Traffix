@echo off
REM Convenience launcher for Windows.
REM TODO: start SUMO, then backend (uvicorn app.main:app --reload),
REM then frontend (npm run dev), each in its own window.
echo TRAFFIX startup script - fill in commands per docs/setup.md
